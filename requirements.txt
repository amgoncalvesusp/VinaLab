# VinaLab core runtime — the minimum needed to launch and run docking.
#
# Target interpreter: CPython 3.10–3.13 (64-bit). Install into a fresh venv:
#
#     python -m venv .venv
#     .venv\Scripts\activate         # Windows
#     source .venv/bin/activate      # Linux/macOS
#     python -m pip install --upgrade pip
#     python -m pip install -r requirements.txt
#
# Notes for reliable cross-machine installs:
#   * numpy is held below 2.0 on purpose — the AutoDock Vina wheel and several
#     dependencies are built against the numpy 1.x ABI.
#   * The heavy ML rescoring stack (torch/dgl/...) lives in
#     requirements-scoring.txt so a missing torch wheel on a newer Python can
#     never block the core install. Install it only if you need RTMScore /
#     DeltaVinaXGB rescoring.
#   * AutoDock Vina Python bindings are optional; when no wheel exists for your
#     platform VinaLab falls back to the bundled CLI in tools/vina/.

PySide6>=6.7,<6.8
PySide6-Addons>=6.7,<6.8
meeko>=0.4
scipy>=1.10
gemmi>=0.6
rdkit>=2023.3.1
openbabel-wheel>=3.1.1
pandas>=1.5
matplotlib>=3.6
mplcursors>=0.5
openpyxl>=3.0
reportlab>=3.6
numpy>=1.23,<2
py3Dmol>=2.0
wheel>=0.40

# Optional — AutoDock Vina Python API (CLI fallback used when absent):
# vina>=1.2
